{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://athanor.dev/schema/relation.schema.json",
  "title": "Athanor Relation",
  "description": "A directed, typed edge connecting two Chunks that captures semantic dependency, causality, contrast, or co-occurrence.",
  "type": "object",
  "required": ["source", "target", "type"],
  "properties": {
    "source": {
      "type": "string",
      "pattern": "^[A-Z]{2,4}-[A-Z]{3,5}-\\d{3}$",
      "description": "The chunk_id of the source (origin) chunk."
    },
    "target": {
      "type": "string",
      "pattern": "^[A-Z]{2,4}-[A-Z]{3,5}-\\d{3}$",
      "description": "The chunk_id of the target (destination) chunk."
    },
    "type": {
      "type": "string",
      "enum": [
        "INSTANTIATES",
        "ENABLES",
        "LEARNED_FROM",
        "CONTRASTS_WITH",
        "HARDCODED_EXCEPTION",
        "EXPRESSED_THROUGH"
      ],
      "description": "The semantic type of the relation."
    },
    "description": {
      "type": "string",
      "description": "Optional human-readable explanation of the connection."
    }
  },
  "additionalProperties": false
}
